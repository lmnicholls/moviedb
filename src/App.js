import "./AppStyles.js";
import { AppContainer } from "./AppStyles.js";
import Header from "./components/Header/Header";

const App = (props) => {
  return (
    <AppContainer>
      <Header />
    </AppContainer>
  );
};

export default App;
